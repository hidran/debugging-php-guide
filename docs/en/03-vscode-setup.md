# VS Code: IntelliSense + Debug
- Install VS Code and the PHP Extension Pack.
- Set the PHP executable path.
- Create `.vscode/launch.json` with:
  - "Listen for Xdebug" (port 9003)
  - "Launch currently open script" for CLI debugging.
